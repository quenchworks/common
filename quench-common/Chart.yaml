apiVersion: v2
name: quench-common
description: Quenchworks shared Helm library chart. Labels, hardened security contexts, a digest-only image resolver, and shared pod-spec knobs (scheduling, lifecycle, probes, extensibility).
type: library
version: 0.0.1
appVersion: "0.0.1"
icon: https://raw.githubusercontent.com/quenchworks/.github/main/profile/assets/chart-icon.png
maintainers:
  - name: mkabumattar
home: https://quenchworks.mkabumattar.com
sources:
  - https://github.com/quenchworks/common
# Published as an OCI artifact to oci://ghcr.io/quenchworks/charts/quench-common.
# App charts depend on it via repository: oci://ghcr.io/quenchworks/charts.
